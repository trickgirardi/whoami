---
import BasicLayout from "./BasicLayout.astro";

interface Props {
  title: string;
  description: string;
  fullScreen?: string;
}

const { title, description } = Astro.props;
---

<BasicLayout
  description={description}
  title={title}
>
  <slot />
</BasicLayout>
