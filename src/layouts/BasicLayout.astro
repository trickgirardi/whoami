---
import "../style.css";
import { ClientRouter } from "astro:transitions";
import { Font } from "astro:assets";
import { LINKS } from "../lib/constants";
import { SITE } from "../site-config";
import GridTransition from "../components/GridTransition.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
const image = `${Astro.url.origin}/og-image.png`;
const siteDomain = new URL(SITE.site.url).hostname;

const schema = {
  "@context": "https://schema.org",
  "@type": "Person",
  name: SITE.author.fullName,
  url: Astro.url.origin,
  sameAs: [LINKS.linkedin, LINKS.github],
  image: `${Astro.url.origin}/og-image.png`,
  jobTitle: SITE.author.jobTitle,
  knowsAbout: ["JavaScript", "React", "Astro", "Node.js", "Web Development"],
  worksFor: {
    "@type": "Organization",
    name: "Self-Employed",
    address: {
      "@type": "PostalAddress",
      addressLocality: SITE.location.countryName,
      addressCountry: SITE.location.countryCode,
    },
  },
  nationality: {
    "@type": "Country",
    name: SITE.location.countryName,
  },
};
---

<!doctype html>
<html
  lang="en"
>
  <head>
    <meta charset="UTF-8" />
    <Font cssVariable="--font-cabinet-grotesk" preload />
    <Font cssVariable="--font-satoshi" preload />
    <ClientRouter />
    <script is:inline>
      try {
        if (localStorage.getItem("portfolioMode") === "light") {
          document.documentElement.classList.add("style-paper");
        }
      } catch {}
    </script>
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta content={Astro.generator} name="generator" />
    <title>{title}</title>
    <meta content={description} name="description" />
    <meta content="index, follow" name="robots" />
    <meta name="theme-color" content="#171717" />
    <meta name="author" content={SITE.author.fullName} />
    <meta
      name="keywords"
      content={`full stack developer, web developer, React, Astro, Solid.js, Svelte, freelance developer, ${SITE.location.countryName}, UX, animations, micro-interactions`}
    />
    <meta content="website" property="og:type" />
    <meta content={title} property="og:title" />
    <meta content={description} property="og:description" />
    <meta content={Astro.url.origin} property="og:url" />
    <meta property="og:site_name" content={SITE.author.fullName} />
    <meta property="og:locale" content="en_US" />
    <meta
      content={image || `${Astro.url.origin}/og-image.png`}
      property="og:image"
    />
    <meta property="og:image:alt" content={description} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta content="summary_large_image" name="twitter:card" />
    <meta content={title} name="twitter:title" />
    <meta content={description} name="twitter:description" />
    <meta
      content={image || `${Astro.url.origin}/og-image.png`}
      name="twitter:image"
    />

    <link href={Astro.url.href} rel="canonical" />
    <script
      is:inline
      set:html={JSON.stringify(schema)}
      type="application/ld+json"
    />
    <script
      defer
      is:inline
      src="https://cloud.umami.is/script.js"
      data-website-id={import.meta.env.UMAMI_WEBSITE_ID}
      data-domains={siteDomain}></script>
  </head>
  <body class="bg-darkslate-700 flex flex-col justify-center items-center">
    <script is:inline>
      try {
        if (localStorage.getItem("portfolioMode") === "light") {
          document.body.classList.add("style-paper");
        }
      } catch {}
    </script>
    <GridTransition />
    <slot name="loader" />
    <slot />
  </body>
</html>

<script>
  import {
    PORTFOLIO_HAS_VISITED_NON_HOME_KEY,
    PORTFOLIO_LAST_PATH_KEY,
    normalizePathname,
  } from "../lib/nav-path";

  if (typeof document !== "undefined") {
    document.addEventListener("astro:page-load", () => {
      const loader = document.querySelector(".loader");
      if (loader instanceof HTMLElement) {
        loader.style.opacity = "0";
        loader.style.pointerEvents = "none";
      }

      const path = normalizePathname(window.location.pathname);
      if (path !== "/") {
        sessionStorage.setItem(PORTFOLIO_HAS_VISITED_NON_HOME_KEY, "1");
      }

      queueMicrotask(() => {
        sessionStorage.setItem(PORTFOLIO_LAST_PATH_KEY, path);
      });
    });
  }
</script>

<style is:global>
  body {
    margin: 0;
    font-family: var(--font-satoshi);
    -webkit-font-smoothing: antialiased;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: var(--font-cabinet-grotesk);
  }
  p {
    font-family: var(--font-satoshi);
  }

  b {
    font-weight: 700 !important;
  }

  @supports (view-transition-name: none) {
    ::view-transition-old(root),
    ::view-transition-new(root) {
      animation-duration: 320ms;
      animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    }
  }
</style>
